# Optional: signing secret for the auth cookie. If unset, a dev-only constant is used.
AUTH_SECRET=replace-with-a-long-random-string

# Azure AI Agent Service — fill in when wiring the real agent in app/api/chat/route.ts
AZURE_AI_ENDPOINT=
AZURE_AI_KEY=
AZURE_AI_AGENT_ID=
